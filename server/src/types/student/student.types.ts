export type StudentViewType = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  profileImageUrl: string;
  address: string;
  mainLanguage: string;
  createdAt: Date;
  role: string;
};
